{
  "name": "skyroute-planner-root",
  "version": "1.0.0",
  "description": "Script raíz para levantar frontend y backend simultáneamente",
  "scripts": {
    "dev": "concurrently \"npm run dev:back\" \"npm run dev:front\"",
    "dev:back": "cd backend && .\\venv\\Scripts\\python.exe main.py",
    "dev:front": "cd frontend && npm run dev",
    "install:all": "npm install && cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
